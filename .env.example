# =============================================================================
# OpenClaw Railway Template — Environment Variables
# =============================================================================
# Copy to .env and fill in the values you need.
# Only SETUP_PASSWORD and TAILSCALE_AUTHKEY are required for deployment.
# Everything else is optional and gracefully degrades if not set.
# =============================================================================

# --- Required ---
SETUP_PASSWORD=                    # Password to access the /setup wizard (HTTP Basic auth)
TAILSCALE_AUTHKEY=                 # Tailscale auth key (reusable + ephemeral, pre-approved)

# --- Tailscale ---
TAILSCALE_HOSTNAME=openclaw-railway  # Hostname on your tailnet
TAILSCALE_SERVE=true               # Enable Tailscale Serve HTTPS proxy (default: true)

# --- LLM Providers (set at least one, or enter during setup wizard) ---
ANTHROPIC_API_KEY=                 # Anthropic API key (Claude models)
OPENAI_API_KEY=                    # OpenAI API key (GPT, Codex, embeddings)
OPENROUTER_API_KEY=                # OpenRouter API key (multi-model routing, cheap heartbeats)
DEEPSEEK_API_KEY=                  # DeepSeek API key (cheap reasoning for subagents)
GROK_API_KEY=                      # xAI Grok API key (trending/social data)

# --- GitHub ---
GITHUB_TOKEN=                      # GitHub PAT for repo access from the instance

# --- Gateway ---
OPENCLAW_GATEWAY_TOKEN=            # Gateway auth token (auto-generated if not set)

# --- n8n Webhook Bridge ---
OPENCLAW_HOOKS_TOKEN=              # Shared secret for webhook auth (OpenClaw <-> n8n)
N8N_WEBHOOK_URL=                   # n8n internal URL (e.g. http://Primary.railway.internal:5678)

# --- Composio (500+ SaaS Integrations via Rube MCP) ---
COMPOSIO_API_KEY=                  # Composio API key (https://composio.dev)

# --- Modal (Serverless GPU/Compute) ---
MODAL_TOKEN_ID=                    # Modal token ID (https://modal.com)
MODAL_TOKEN_SECRET=                # Modal token secret (pair with MODAL_TOKEN_ID)

# --- Observability: Langfuse (LLM Tracing + Evals) ---
LANGFUSE_PUBLIC_KEY=               # Langfuse public key (https://langfuse.com)
LANGFUSE_SECRET_KEY=               # Langfuse secret key
LANGFUSE_BASEURL=                  # Langfuse host (default: https://cloud.langfuse.com)

# --- Observability: PostHog (Product Analytics) ---
POSTHOG_API_KEY=                   # PostHog project API key (https://posthog.com)
POSTHOG_HOST=                      # PostHog ingest host (default: https://us.i.posthog.com)

# --- Observability: Generic OTLP (APM) ---
OTEL_EXPORTER_OTLP_ENDPOINT=      # OTLP endpoint (e.g. http://tempo:4318 for Grafana Tempo)

# --- State Directories (Railway volume mount) ---
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace
